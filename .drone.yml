kind: pipeline
name: default

steps:
- name: build
  image: gradle:6.6.1-jdk11
  commands:
  - gradle build
  
- name: publish to github
  image: plugins/github-release
  settings:
    api_key:
      from_secret: github_token
    files: build/libs/*
  when:
    event: tag
